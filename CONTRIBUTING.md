# Contributing

## Local Setup

```bash
npm install
npm run build
```

Copy `.env.example` to `.env` only for local development and keep real values private.

## Guidelines

- Keep changes small and focused.
- Do not commit generated files such as `dist/`.
- Do not commit credentials or private data.
- Update documentation when setup or behavior changes.
- Run the build before opening a pull request.

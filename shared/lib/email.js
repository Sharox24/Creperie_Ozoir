export async function sendEmail(to, subject, html) {
  // Placeholder: Implement via Supabase Edge Function or server
  // This function is intentionally a no-op in the frontend.
  // Suggested implementation: call a secure API endpoint that sends email via SMTP/Resend.
  return { ok: true };
}

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Google data sync
# A Google Places API key with Places API enabled
GOOGLE_PLACES_API_KEY=

# Optional: Supabase service role (only for server-side scripts)
# Do not expose in the client. Use locally or on a secure server/CI.
SUPABASE_SERVICE_ROLE=

# Optional email notifications (to implement via Edge Function or server)
# NOT used directly by the frontend; document-only placeholders
# MAIL_FROM=
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=

